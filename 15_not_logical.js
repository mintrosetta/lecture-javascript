/* 
    Not
        ใช้สำหรับกลับด้านค่าจริงเท็ต
        เช่น
            let x = true;
            console.log(!x); // result is false
*/

let temp = -15;

if (!(temp > 0)) {
    console.log("It's warm outside");
} else {
    console.log("It's cool outside");
}